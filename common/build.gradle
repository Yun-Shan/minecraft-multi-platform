plugins {
    id 'java'
}

repositories {
    mavenCentral()
}

dependencies {
    // 这个没有塞进libs.versions.toml管理的必要，所以就直接写在这里，避免libs.versions.toml中的无用内容过多
    compileOnly "org.jetbrains:annotations:26.0.2"
}